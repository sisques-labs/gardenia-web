# Spec: auth-redesign

## Summary

| Capability | Type | Description |
|---|---|---|
| `auth-ui` | New | Layout shells + 9 atomic components for the auth presentation layer |
| `forgot-password` | New | Full vertical slice: port → use case → schema → hook → screen |

Both capabilities implemented as full specs and synced to openspec/specs/auth/

---

See: `openspec/specs/auth/auth-ui.spec.md` and `openspec/specs/auth/forgot-password.spec.md`
