change: inventory-table-redesign
status: apply
created: 2026-07-03
artifact_store: hybrid
context: inventory
phases:
  explore: skipped
  proposal: done
  spec: done
  design: done
  tasks: done
  apply: in_progress
  verify: partial
  archive: pending
next_recommended:
  - sdd-apply
notes: |
  Five chained PRs. PR1 (row actions dropdown + delete confirmation) and PR2
  (server-side pagination + sorting) done. PR3 (server-side filtering + quick
  filter chips) done: useInventoryFilters now debounces search and emits
  InventoryFilter[] against the API instead of filtering an in-memory array;
  ActiveFilterChips wired into InventoryFilters; page-reset-on-filter-change
  effect ported from plants. pnpm test (250 suites/1227 tests), tsc --noEmit,
  eslint all green. PR4 pending. PR5 (bulk selection + bulk delete) is blocked
  on the companion sisques-labs/gardenia-api change `inventory-bulk-delete`,
  which has already shipped `inventoryItemsDeleteBulk` on the same branch.
