change: inventory-table-redesign
status: apply
created: 2026-07-03
artifact_store: hybrid
context: inventory
phases:
  explore: skipped
  proposal: done
  spec: done
  design: done
  tasks: done
  apply: in_progress
  verify: partial
  archive: pending
next_recommended:
  - sdd-apply
notes: |
  Five chained PRs. PR1-PR3 done (row actions + delete confirm, server-side
  pagination/sorting, server-side filtering + chips). PR4 (item detail drawer)
  done: InventoryItemDetailDrawer + "View detail" dropdown item, wired to the
  already-loaded InventoryItem object (deliberately skipped a redundant
  findById fetch — see tasks.md 4.1 for rationale). pnpm test (251
  suites/1232 tests), tsc --noEmit, eslint all green. PR5 (bulk selection +
  bulk delete) pending — blocked on the companion sisques-labs/gardenia-api
  change `inventory-bulk-delete`, which has already shipped
  `inventoryItemsDeleteBulk` on the same branch.
